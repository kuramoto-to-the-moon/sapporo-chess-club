---
import UpcomingScheduleList from "./UpcomingScheduleList.svelte";
import { t, getLocalePath, type Locale } from "@/i18n";
import { getSortedScheduleDates } from "@/lib/schedule-data";

interface Props {
  locale: Locale;
}

const { locale } = Astro.props;
const i = t(locale);

const dates = await getSortedScheduleDates();
const scheduleHref = getLocalePath(locale, "/schedule");
---

<section id="schedule" class="px-5 py-6">
  <h2 class="text-sm font-semibold uppercase tracking-wider text-[#737373] mb-4 flex items-center gap-2">
    <span class="inline-block w-1 h-3 bg-[#2563eb]"></span>
    {i.schedule.label}
  </h2>
  <UpcomingScheduleList dates={dates} locale={locale} scheduleHref={scheduleHref} client:only="svelte" />
</section>
